= module Kernel

모든 클래스에서 참조할 수 있는 메소드가 정의되어있는 모듈입니다.
[[c:Object]] 클래스는 이 모듈을 include하고 있습니다.
#@#[[unknown:내장 함수]] 문서에서 설명하는 메소드들은 이 모듈에 정의되어있습니다.

[[c:Object]] 클래스의 메소드들은 이 모듈에 정의되어있습니다.これはトップレベルでのメソッドの再定義に対応するためです。

#@include(functions)
#@include(specialvars)
